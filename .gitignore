# All CSV files (experiment data)
*.csv
# But keep any CSV files that might be needed (if any)
# !important_data.csv

# All JSON files (optimization history, test outputs, etc.)
*.json
# But keep configuration JSON files
!package.json
!package-lock.json
!tsconfig.json
!metadata.json
# Keep any other config JSON files if needed
# !config.json

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Keep configuration JSON files (package.json, tsconfig.json, etc.)
!package.json
!package-lock.json
!tsconfig.json
!metadata.json

# PNG 图像文件（可视化图表）
*.png
**/*.png

# PNG 图像文件（可视化图表）
*.png
**/*.png
